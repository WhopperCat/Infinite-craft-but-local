{
  "name": "infinite-craft-local",
  "version": "1.0.0",
  "private": true,
  "description": "In-browser Infinite Craft clone running Gemma via WebLLM",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
